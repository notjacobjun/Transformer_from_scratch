import torch
from torch import nn

class MultiHeadAttention(nn.Module):
	def __init__(self, query: torch.Tensor, keys: torch.Tensor) -> int:

	def forward(self, query: torch.Tensor, keys: torch.Tensor) -> int:

